---
applications:
- name: GoFNOL-blue
  memory: 256M
  buildpacks:
    - newrelic_dotnet_core_buildpack
    - sol-dotnet_core_buildpack-latest
  routes:
  - route: gofnol-[ENV]-blue.[DOMAIN]
  services:
  - EAI
  - IS
  - NGP
  - a2e-data